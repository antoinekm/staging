{
  "name": "staging-root",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "format": "pnpm -r format"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "eslint-config-antoine": "^1.0.14",
    "prettier": "^3.3.3",
    "typescript": "^5.3.2"
  }
}